# Run 5

**Written 2026-09-09**, after run 4 and the identity finish. Baseline: `main` at `2186dcbd3`.
[`2026-09-08-parallel-worktree-execution.md`](./2026-09-08-parallel-worktree-execution.md) still
governs.

---

## Where run 4 left things

Shipped all five — **I2, SK1, PV2, MR2, CR1** — then **WF2**, **RB1**, **I3** and **I4** followed on
their own, plus the landing-page rebuild.

The consequential result is that **the identity chain is complete**: I2 put Keycloak behind the same
`requireTenant` seam, I3 gave every subject a durable internal user id with membership under RLS, and
I4 made `readAlicornBearer` present the signed-in session with no silent fallback to `local`. That was
the block on **OP1**, and OP1 is the biggest remaining fan-out in the backlog.

CR1 landed the extraction, which unblocks **CR2 → IV1**. RB1 landed the Rulebook, which unblocks
**RB2**. SK1 landed stable stage keys, which unblocks **AT1**.

## Run 5 — five slots

Two cloud tickets, so `c3` is spare this run.

| #   | Branch                   | Ticket  | Why now                                                                                                                                                          | Slot |
| --- | ------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---- |
| 1   | `alc-op1-org-platform`   | **OP1** | Orgs, invites, seats. Identity is done, so this is finally real rather than `local`-only. Unblocks OP2, OP3 → PS1 → SP1, and PA1. Five tickets behind one. 3 ew. | c1   |
| 2   | `alc-cr2-contract-gate`  | **CR2** | CR1 just landed the registry rows. CR2 is the required check that makes a breaking contract change stop a run. Unblocks IV1. 2 ew.                               | c2   |
| 3   | `alc-at1-composed-teams` | **AT1** | SK1 done. Foreman composes a team from member roles and a human gates the composition. 4 ew, largest in the batch.                                               | —    |
| 4   | `alc-rb2-learning-edge`  | **RB2** | RB1 done. The learning edge reaches the splitter — the lead's preamble carries accepted rules. 0.5 ew, smallest.                                                 | —    |
| 5   | `alc-r2-cli-rename`      | **R2**  | The rebrand chain, unstuck. See below. 1 ew.                                                                                                                     | —    |

**Start 4 and 1 first** — RB2 because it is half a week, OP1 because everything downstream waits on it.

### Migration numbers — reserved before dispatch

`SCHEMA_VERSION` is **41**.

| Version | Reserved for                                                                                                                                                                  |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| v42     | AT1 — composed-team record, if it needs client-side state. **Not used: AT1 kept the roster in the Feature Journal and the verdict on the existing gate row, so v42 is free.** |
| v43     | RB2 — if the preamble needs a persisted rule selection                                                                                                                        |
| v44     | unallocated                                                                                                                                                                   |

OP1 and CR2 are server-side only and take no SQLite version.

### Held back deliberately

- **UI5** — still behind R5. Unchanged.
- **IV1, PA1, OP2/OP3/PS1/SP1** — behind this run's tickets by one hop.
- **MB1/MB2** — unblocked but v2.0, and the ROADMAP calls the mailbox the least certain estimate in
  the plan. Unblocked is not due.
- **I5, BC1** — the circular dependency in §2 of the sequencing plan is still unresolved, and it is a
  decision, not engineering. See below.

---

## R2 proceeds without R1's artwork — stated as an assumption

Run 3 and run 4 both raised this and neither got an answer, so this run acts on it.

R1's only remaining work is brand artwork: one `logo.svg` at two paths and tray PNGs at 22×14 and
44×28. **R2 — renaming the CLI binary and shipping the `orca` shim — needs none of it.** It is blocked
on R1 by the ticket's dependency line and by nothing real. Behind R2 sit R3, R4 and then R5, the
upstream cut, whose meter runs at roughly 500 upstream PRs per release while we wait.

So R2 runs this batch. If the artwork lands meanwhile, R1 closes independently; nothing about R2 has
to be redone. **Say so if you want it held** — it is reversible until R3 starts renaming call sites.

R4, the 886-identifier codemod, is explicitly _not_ in this run: CLAUDE.md says a piecemeal rename
makes the CI grep gate lie, so it wants a quiet tree, and this run has four other branches in flight.

---

## The I5 ↔ BC1 cycle is the one thing still genuinely stuck

- **I5** — Control API mints relay tokens — _depends on I2 ✓ and **BC1**_
- **BC1** — relay on Alicorn infrastructure — _depends on **I5**_

I2 is now done, so I5's other dependency is satisfied and this cycle is the only thing holding it. The
sequencing plan proposes breaking it in three steps — BC1a (stand the relay up in staging on existing
auth) → I5 (mint ES256 tokens, publish JWKS) → BC1b (point the relay at our JWKS, cut production over).
That shape has not been confirmed. It is a decision about production cutover, so it stays with you.

---

## Still yours, not engineering's

|                                  |                                                                                                                                       |
| -------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| **DS2** — Windows EV certificate | `urgent`, weeks of vetting, still not started. Longest lead item in the roadmap, and every unstarted day is a day on the end of v0.1. |
| **DS1** — Apple Developer ID     | Enrolment lead time.                                                                                                                  |
| **R1** — brand assets            | Specs in the rebrand plan. No longer blocking R2 (above).                                                                             |
| **SM1** — the threshold          | `MEASUREMENT.md` §10, twelve rows, "Agreed value" still empty. The sign-off commit must precede the run's first ledger row.           |
| **LG5** — what "completed" means | Shipped with both denominators visible and the swap seam documented; picking the rule is yours.                                       |
| **I5 / BC1**                     | The cutover shape above.                                                                                                              |
| **Plane**                        | Its MCP server has been failing to connect. Run 4's eight completed tickets plus I3, I4, WF2 and RB1 are unrecorded.                  |
